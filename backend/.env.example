# Environment variables for Dormo backend
DEBUG=True
HOST=0.0.0.0
PORT=8000

# Database (when you add one later)
# DATABASE_URL=sqlite:///./dormo.db

# Email settings (for contact form)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
